# Base de datos (ya en la nube)
DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=

# JWT (auth_service)
JWT_SECRET=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Puertos locales (docker-compose)
API_GATEWAY_PORT=8000
AUTH_SERVICE_PORT=8001
INVENTORY_SERVICE_PORT=8002
ALERTS_SERVICE_PORT=8003
ACTIVITIES_SERVICE_PORT=8004
REPORTS_SERVICE_PORT=8005
TIMER_SERVICE_PORT=8006

# RabbitMQ (opcional)
RABBITMQ_USER=kryotec
RABBITMQ_PASS=kryotec2024

# CORS / Frontend
FRONTEND_ORIGIN=http://localhost:5173
CORS_ADDITIONAL_ORIGINS=

# Cliente (Vite) - se usan en build del Docker del cliente
VITE_API_URL=http://localhost:8000
VITE_TIMER_WS_URL=ws://localhost:8006/ws/timers

# API Gateway: URLs (para producción en Railway)
AUTH_SERVICE_URL=
INVENTORY_SERVICE_URL=
ALERTS_SERVICE_URL=
ACTIVITIES_SERVICE_URL=
REPORTS_SERVICE_URL=
